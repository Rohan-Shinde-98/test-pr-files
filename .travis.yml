os: linux
arch: ppc64le
distro: bionic

jobs:
  include:
    - stage: Check PR
      if: branch = main AND type = pull_request
      script: 
        - python3 script.py $TRAVIS_PULL_REQUEST
        - ls -la
        - chmod 777 changed_files.txt
        - cat changed_files.txt
